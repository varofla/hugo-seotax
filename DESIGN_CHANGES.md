# 2026-09-21 디자인 통합 결과

사용자가 DESIGN_AUDIT.md의 디자인 결정을 위임한 뒤 구현한 첫 리디자인이다. 운영 배포는 하지 않았으며 개발 테마에 적용했다.

## 채택한 방향

- About·포스팅 상세·사이드바를 기준으로 흰 본문, 청록색 강조, 옅은 구분선, 조용한 메타데이터를 공통 규칙으로 삼았다.
- 글 목록은 카드 테두리·그림자를 없앤 행 형태다. 제목의 굵기를 높였고, 데스크톱에는 오른쪽 4:3 썸네일, 작은 모바일에는 제목 아래 16:9 썸네일을 둔다. 행 클릭과 개별 태그·카테고리 링크를 유지한다.
- 전체 카테고리는 상위 주제별 디렉터리다. 데스크톱 2열, 모바일 1열이며 하위 분류는 바로 이동할 수 있게 모두 보인다. 사이드바의 펼침 방식은 그대로다.
- 태그는 글이 많은 순으로 정렬한 링크 목록이다. 데스크톱 3열, 작은 화면 2열이며 태그마다 글 수를 표시한다. 태그 결과는 기존처럼 검색 페이지로 연결한다.
- 탐색 제목과 수량은 `전체 글`, `카테고리`, `태그`, `검색`, `글 n개`로 통일했다. About의 프로젝트명·Skills/Projects 제목과 사용자가 작성한 카테고리 이름은 유지한다.
- Pretendard 1.3.9 variable font를 자체 호스팅한다. 라이선스를 함께 포함했고, UI와 본문에 같은 서체를 사용한다. 고정폭 메타데이터에는 시스템 고정폭 서체와 한국어 fallback을 명시했다.

## 바뀐 화면과 기능

| 대상 | 결과 |
| --- | --- |
| 홈·카테고리·검색 목록 | 동일한 글 행, 제목·요약·메타데이터 위계, 정렬과 여백 |
| 탐색 진입점 | 프로필은 전체 글로 연결하고, 사이드바 상단에는 카테고리·태그 아이콘과 About만 둔다. 해당 위치는 활성 상태로 표시한다. |
| 전체 카테고리 | 본문 전용 템플릿으로 분리. 중복 ID와 비활성 하위 링크 제거 |
| 카테고리별 목록 | 실제 글 수 표시. 상위 카테고리의 하위 주제 바로가기는 사이드바가 접히는 화면에서만 보이고, 하위 카테고리에는 상위 링크 |
| 태그 | 빈 태그를 제외한 85개 태그. 글 메타데이터·검색 JSON도 동일한 정규화 사용 |
| 검색 | 모달과 페이지의 입력·필터·드롭다운·선택 칩·focus 상태 통일. 필터 기능과 URL 계약 유지 |
| 검색 상태 | 모달의 초기 안내·로드 실패 안내, 검색 페이지의 빈 결과와 초기화 링크 |
| 포스팅 | 기존 읽기 배치·목차·전환 유지. 진행선·series 강조색·이전/다음·footer 조정 |
| About | 기존 갤러리 유지. 링크 색상 상속 충돌 및 복귀 스크립트 실행 순서 수정 |
| 모바일 | 메뉴/목차/검색 구조 유지. 목차 없는 화면의 상단 바에도 페이지 문맥 표시 |
| 404·posts section | 404는 공통 골격과 검색/목록 복귀 링크. `/posts/`도 공통 글 목록 사용 |

검색 입력과 필터 선택은 기존처럼 즉시 결과를 갱신하고 검색 실행 시 URL에 반영한다. 검색 결과 페이지 번호 링크에도 필터 상태가 보존된다. 카테고리 2단계 경로, 다중 태그 AND/OR, 이미지 확대, About 미리보기, 메뉴·목차 전환 기능을 유지했다.

## 코드 구조

- `variables/_colors.scss`: 의미별 색상과 서체 토큰. 기존 shortcode 변수는 새 토큰의 별칭으로 유지한다.
- `variables/_fonts.scss`, `static/fonts/`: 자체 호스팅 폰트와 라이선스.
- `_lists.scss`: 목록·분류·탐색 헤더·페이지 번호·빈 결과·404.
- `_search.scss`: 검색 입력·모달·필터. 기존 공용 글 목록 스타일은 분리했다.
- `_reading.scss`, `_about.scss`: 이전 `_custom.scss`에서 읽기와 About의 책임을 분리했다. `_custom.scss`는 추가 스타일용으로 남겼다.
- `categories/catalog`, `categories/ordered-keys`: 사이드바와 디렉터리가 같은 분류 순서와 글 수를 공유한다.
- `content/tags`: 공백 제거·빈 값 제외·동일 태그 중복 제거. 원본 포스팅 파일은 수정하지 않았다.
- `posts/list`: 홈과 `/posts/`의 목록 렌더링을 공유한다.
- 목록·검색·분류의 wrapper에서 `.markdown`을 제거했다. 실제 글 본문/shortcode의 Markdown 표현은 유지한다.

## 검증

- Hugo 0.158.0 extended로 개발 사이트의 **전체 104개 글**을 빌드했다. Pages 178, Paginator pages 30, Non-page files 1,862, Static files 81, Processed images 1,899. 기존 이미지 캐시를 임시 디렉터리에 복사해 재사용했다.
- 출력은 `/tmp/varofla-redesign/public`, 이미지/cache도 같은 임시 루트에 있다. 운영 디렉터리를 수정하거나 배포 스크립트를 실행하지 않았다.
- Chrome 151에서 주요 화면 14개 조합을 1440px/390px로 캡처했다. 360/768/1238/1239/1440px에서 카테고리 목록의 문서 가로 넘침을 추가 확인했다.
- 29개 브라우저 확인 항목 통과: 태그 AND/OR의 실제 결과 ID 연결, 검색 페이지 2, 빈 결과, 상위/하위 필터 선택·해제, 검색 실행 URL, 디렉터리 링크, 사이드바 펼침, 글 행 진입/복귀, About 참조 페이지 복귀·갤러리 미리보기, 이미지 확대/Escape, 모바일 패널 상호 배타, 마지막 페이지, `/posts/` 목록, 실제 Pretendard 렌더링, 로컬 스크립트 예외 없음.
- 주요 페이지의 ID 중복 없음, 내부 탐색 링크의 출력 존재, 검색 ID 104개의 순서·참조 범위, 빈 태그 제외를 확인했다. Hobby 수량은 하위 분류를 더하지 않은 실제 글 29개다.
- 검사 스크립트와 화면 캡처는 `/tmp/varofla-redesign`에 있다. `/tmp` 자료는 이후 삭제될 수 있다.

## 다음 피드백에서 살펴볼 부분

이번 선택을 구현한 상태이므로 화면을 보고 조정하면 된다.

- 목록 행의 여백·썸네일 크기와 모바일에서 첫 화면에 보이는 정보량.
- categories의 2열 배치와 tags의 빈도순 목록 밀도.
- 제목·본문·메타데이터의 크기, 새로운 서체의 느낌.
- 검색 모달과 페이지의 필터 배치.

실제 iOS/Android의 키보드·safe area·pinch 감각은 실기기 확인이 남아 있다. 외부 Comentario 댓글 내부 UI와 YouTube 플레이어는 이번 자동화 확인에서 제외했다. 댓글 입력이나 외부 서비스 설정은 변경하지 않았다.
