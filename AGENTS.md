# AGENTS.md

## 먼저 읽을 문서

- 작업 전에 [THEME_CONTEXT.md](THEME_CONTEXT.md)를 읽는다. 현재 구조와 보존해야 할 동작의 단일 기준 문서다.
- 미완료 작업은 [TODO.md](TODO.md)를 확인한다. 특히 About은 갤러리 기반 구현은 끝났지만 콘텐츠와 최종 디자인 결정이 남아 있다.
- 과거 분석 문서 대신 현재 코드, 위 두 문서, Git 이력을 기준으로 판단한다. 문서와 코드가 다르면 코드를 확인하고 문서를 함께 갱신한다.

## 작업 원칙

- 이 테마는 varofla 전용이며 한국어 단일 사이트다. 범용 Hugo 테마 호환성이나 upstream 기능을 자동으로 복원하지 않는다.
- 운영 디렉터리 `/mnt/tank/blog/varofla_blog`와 `deploy.sh`는 건드리지 않는다. 작업 대상은 개발 사본 `/mnt/tank/blog/varofla_blog_dev`다.
- Hugo는 `../../tools/hugo.sh`, `../../tools/preview.sh`로 실행할 수 있다. 기본 검증은 실제 글 1~몇 개를 `/tmp`에 복사한 축소 빌드로 한다. 전체 빌드는 필요성과 비용을 먼저 판단한다.
- 기존 기능 제거, About의 콘텐츠·레이아웃 선택, 모바일 동작 변경은 사용자에게 확인한다.
- 글 전환, 검색 데이터 ID, 2단계 카테고리, 이미지 확대, About 갤러리, 모바일 패널은 서로 연결된 코드가 많다. 관련 계약을 확인한 뒤 작은 단위로 수정하고 대표 화면을 검증한다.

## 문서 관리

- 현재 사양과 구조는 `THEME_CONTEXT.md`, 미완료 항목은 `TODO.md`에만 기록한다.
- 완료된 TODO는 결과가 장기적으로 필요한 경우에만 `THEME_CONTEXT.md`에 반영하고 TODO에서 제거한다. 날짜별 작업 일지는 Git 커밋으로 남긴다.
- `archetypes/*.md`는 문서가 아니라 Hugo 새 글 생성 템플릿이므로 문서 정리 대상으로 삭제하지 않는다.
