# varofla 디자인 일관성 분석

분석일: 2026-09-21. 개발 테마 HEAD: `6a8af83`. 사용자가 기준으로 지정한 최근 About, 포스팅 상세, 카테고리 사이드바를 중심으로 비교했다. 이 문서는 디자인 변경 전의 분석 기록이다. 채택한 구현과 검증 결과는 [DESIGN_CHANGES.md](DESIGN_CHANGES.md)를 따른다.

## 1. 결론

현재 사이트에는 **seotax의 기본 UI, 최근 varofla UI, 두 스타일이 섞인 중간 상태**가 공존한다. 전체를 새로 디자인하기보다 최근 세 화면에서 공통 규칙을 추출하고, 목록·분류·검색·보조 화면에 적용하는 것이 적합하다.

색상만 통일해서 끝낼 수 없는 이유는 다음과 같다.

- 목록·분류·검색 UI까지 `.markdown` 안에 들어가 본문용 제목·링크·목록 규칙을 상속한다.
- 새로운 색상과 폰트가 공통 토큰 대신 페이지별 선언으로 추가됐다.
- 사이드바용 DOM을 일반 페이지에서 재사용하지만 CSS와 JS는 그 재사용을 지원하지 않는다.
- 태그·카테고리 전체 페이지의 탐색 목적과 진입 경로가 충분히 설계되지 않았다.

## 2. 유지할 디자인 언어

| 요소 | 기준 화면에서 읽히는 방향 | 다른 화면에 적용할 원칙 |
| --- | --- | --- |
| 본문 표면 | 흰 배경, 콘텐츠와 이미지가 중심 | 정보 묶음마다 두꺼운 상자나 배경색을 추가하지 않는다 |
| 탐색 표면 | 사이드바의 아주 옅은 차가운 색 배경 | 검색 모달·탐색 패널에서 같은 계열을 쓴다. 본문까지 같은 배경으로 만들 필요는 없다 |
| 강조 | 청록색 `oklch(0.5 0.13 198)` | 현재 위치, hover, 선택, 주요 동작의 공통 강조색으로 정의 |
| 정보 위계 | 진한 제목, 회색 설명, 더 작은 날짜·태그 | 제목/요약/메타데이터 역할별 크기·굵기·색을 공유 |
| 구분 | 옅은 선과 여백 | About의 연도 구분처럼 선·간격으로 묶고, 카드 외곽선은 필요한 곳만 사용 |
| 이미지 | 둥근 모서리, 사진 자체가 주인공 | 이미지 반경과 로딩 상태를 공유하되 썸네일·대표 사진의 비율은 용도별로 유지 |
| 동작 | 글 진입 시 메뉴가 접히고 목차가 나타남 | 탐색/읽기 모드 차이는 의도된 설계로 보존 |

모든 화면의 배치·크기·반경을 똑같이 만드는 것이 목표는 아니다. About 갤러리, 본문, 조밀한 메뉴는 서로 다른 밀도가 필요하다. 같은 역할의 요소가 같은 규칙을 따르도록 정리한다.

## 3. 페이지별 발견 사항

### A. 전체 카테고리 `/categories/` — 최우선, 동작 결함 포함

운영 화면에서 일반 글머리표, 밑줄 링크, 기본 버튼 외관, 닫힌 폴더/열린 폴더 아이콘이 함께 보인다. 왼쪽의 정돈된 사이드바와 바로 옆 본문이 서로 다른 사이트처럼 보이는 가장 뚜렷한 지점이다.

원인은 [categories/index.html](layouts/_partials/categories/index.html)의 `menu/categories` 재사용이다.

- 스타일은 [_menu.scss](assets/css/_menu.scss)의 `.site-menu .menu-categories ...`에 묶여 있어 본문에는 적용되지 않는다.
- [category-expand.js](assets/js/partials/category-expand.js)의 `document.querySelector('[data-menu-categories]')`는 첫 목록만 초기화한다. 뒤에 있는 본문 목록에는 펼침 이벤트가 없다.
- 닫힌 하위 목록의 `inert`·`aria-hidden="true"`는 HTML에 남는다. 본문에서는 숨기는 CSS가 없어 보이지만 비활성 상태다.
- 운영 DOM에서 `category-children-*` ID 7개가 중복됐다. 본문 버튼을 실행해도 `aria-expanded`는 `false`에서 바뀌지 않았다.

**제안:** 카테고리 데이터·정렬은 공유하고 본문용 렌더링은 분리한다. 상위 주제를 제목으로, 하위 카테고리를 글 수가 붙은 링크 목록으로 보여주는 주제별 디렉터리가 적합하다. 공간이 넓은 본문에서 사이드바의 접힘 트리를 그대로 반복할 이유는 약하다. 기본 펼침 여부와 페이지 역할은 리디자인 시 결정한다.

### B. 태그 `/tags/` — 외관과 데이터 모두 정리 필요

- 4px 모서리의 회색 블록이 화면 전체에 반복된다. 포스팅의 작고 조용한 `#태그`와 시각적 무게가 크게 다르다.
- 태그 페이지에만 큰 채움형 태그 아이콘이 제목 앞에 붙는다. 다른 목록 헤더와 규칙이 다르다.
- 운영 화면에서 **이름 없이 `(67)`만 있는 태그**가 가장 먼저 나온다. 개발 콘텐츠 104개 글 중 67개에서 빈 문자열 태그를 확인했다. 태그 템플릿이 이를 그대로 집계한다.
- 태그별 글 목록은 독립 term 페이지가 아니라 `/search/?tags=...`로 연결된다. `disableKinds: [term]`이므로 개별 태그 페이지가 없는 것은 현재 구조의 의도다.
- 전체 태그 페이지와 검색 데이터가 각각 `pages/posts`, `pages/search`를 사용하므로 `searchExclude`가 생기면 표시 수와 결과 수가 달라질 수 있다. 현재 차이가 발생했다는 뜻은 아니다.

**제안:** 흰 바탕의 가벼운 태그 링크와 작은 글 수로 바꾸고, 태그 전체를 훑어보는 방식(빈도순 유지, 이름순, 그룹화 중 선택)을 명시한다. 빈 태그 정규화는 템플릿·검색 JSON·글 메타데이터 출력의 공통 규칙으로 처리해야 한다. `/tags/`만 숨기면 다른 위치에 빈 태그가 남는다.

근거: [tags/terms.html](layouts/tags/terms.html), [_search.scss](assets/css/_search.scss)의 `.taxonomy-chips`·`.markdown .taxonomy-link`, [tags.json](assets/data/tags.json).

### C. 홈·카테고리별 글 목록 — 새 색을 입힌 카드 중심 구조

세 목록(홈, 카테고리, 검색 결과)은 이미 [post-item.html](layouts/_partials/post-item.html)을 공유한다. 별도 카드 구현 세 개를 새로 만들 필요가 없다.

- 본문·About은 선과 여백 중심인데, 목록은 글마다 14px 반경·1.5px 테두리·그림자가 있는 큰 카드다.
- 데스크톱 썸네일은 200×170px이고 이미지가 있으면 텍스트 영역도 최소 170px 높이를 갖는다. 짧은 설명에서는 메타데이터 위 여백이 커진다.
- 목록 제목은 19px/400, 글 제목은 30px/700, About 프로젝트 이름은 22px/700이다. 크기 차이는 용도상 타당하지만 목록 제목의 낮은 굵기는 콘텐츠보다 카드 외곽을 강조한다.
- 카드와 이미지 모서리는 각각 14px/4px, About 이미지 모서리는 8px이다. 각각의 목적을 설명하는 공통 규칙이 없다.
- 제목 왼쪽 기준선이 헤더(10px 안쪽), 카드 외곽, 카드 내용(22px 안쪽)으로 나뉜다.
- `Blog Posts / Total`, `Categories / Total`, `Search / 검색 결과`, `태그 / 전체 태그`가 섞여 있다. 영문 사용 자체가 문제라기보다 제목·단위·아이콘의 원칙이 없다.

**제안:** 제목이 주인공인 목록 행을 기본안으로 검토한다. 카드 유지도 가능하지만 테두리·그림자를 약하게 하고 밀도를 조정한다. 썸네일과 태그는 유지하며, 카드 전체 클릭·제목 링크·태그 링크가 각각 의도대로 작동하게 한다. 목록 행 전환은 디자인 제안이며 확정 사항이 아니다.

근거: [index.html](layouts/index.html), [categories/list.html](layouts/categories/list.html), [_search.scss](assets/css/_search.scss)의 `.post-item`, `.post-content-area`, `.post-text-area`, `.post-cover`.

### D. 검색 모달·검색 결과 — 새 입력창과 이전 필터가 혼재

검색 모달의 배경과 청록색은 사이드바와 이미 같은 방향이다. 모달을 전부 버릴 필요는 없다.

- 주 검색창은 흰 pill 형태·약한 테두리·청록색 focus를 사용한다.
- 그 아래 필터는 진한 회색 테두리·4px 모서리의 사각 입력창이며 focus가 기존 파란색이다.
- 선택된 필터 chip은 기존 파란 배경/흰 글자다. 드롭다운은 새 색 체계여서 한 제어 안에서도 상태별 디자인이 다르다.
- 검색 결과 헤더, 필터 영역, 결과 목록의 폭·정렬·세로 간격이 개별 선언이다. 빈 결과는 큰 문장 하나여서 다음 행동이 분명하지 않다.
- [input.js](assets/js/search/input.js)와 [list.js](assets/js/search/list.js)가 각각 필터 UI를 생성한다. 외관만 한쪽을 바꾸면 모달과 결과 페이지가 다시 갈라진다.

**제안:** 입력창·선택 chip·드롭다운·해제·focus를 하나의 필터 컴포넌트 규칙으로 묶는다. 모달은 빠른 찾기, 결과 페이지는 탐색이라는 밀도 차이는 유지한다. 카테고리 2단계·다중 태그·모두 일치 기능과 URL 상태를 보존한다. 필터를 접거나 기능을 덜어내는 것은 별도 결정이다.

### E. 카테고리 제목과 수량 — 시각보다 먼저 의미 정리

[categories/list.html](layouts/categories/list.html)은 상위 카테고리의 `Total`을 **글 수 + 하위 카테고리 수**로 계산한다. 반면 하위 카테고리의 `Total`은 글 수이며 전체 `/categories/`의 `Total`은 상·하위 카테고리 수다. 같은 표시가 세 가지 단위를 뜻한다.

**제안:** `글 n개`, `하위 주제 n개`처럼 단위를 구분한다. 현재 집계 의미 변경은 사용자 결정이 필요한 항목이므로 분석 단계에서 변경하지 않는다. 긴 상위/하위 제목은 모바일에서 각각의 맥락이 읽히는지도 확인한다.

### F. 포스팅 — 헤더·읽기 레이아웃은 유지, 삽입 요소와 하단 점검

포스팅 전체가 새 디자인으로 통일된 것은 아니다. 현재 장점인 헤더·목차·읽기 배치를 유지하면서 남은 요소를 점검하는 편이 좋다.

- 실제 스마트 굴비 글의 series 박스는 큰 파란 책갈피 장식과 파란 링크를 사용한다. 바로 옆 청록 목차와 충돌한다.
- 페이지 상단 읽기 진행선은 `#1e6ef4`로 고정되어 있다.
- 이전/다음은 회색 배경의 서로 붙은 상자이고, 긴 제목은 한 줄 말줄임이다. 목록·About과 같은 링크/표면 규칙으로 조정할 수 있다.
- 댓글은 외부 Comentario이며 테마는 light 설정만 한다. 실제 댓글의 글꼴·버튼·여백은 외부 스타일 범위도 함께 확인해야 한다.
- hint의 정보/경고/위험 색이나 코드 문법 색은 의미가 있으므로 청록색 하나로 일괄 변경하지 않는다.

근거: [_shortcodes.scss](assets/css/_shortcodes.scss)의 `.sc-series`, [_layouts.scss](assets/css/_layouts.scss)의 `.scroll-progress-bar`·`.prev-next-wrap`, [footer/comments.html](layouts/_partials/footer/comments.html).

### G. About·사이드바 — 디자인 기준이지만 공통화 대상은 남음

About 갤러리와 사이드바 배치는 기준으로 유지한다. 코드와 실제 효과가 다른 작은 지점은 있다.

- `.about-link-btn`에 새 회색을 선언했지만 전역 `a[href]`의 선택자 우선순위가 더 높다. 실제 GitHub/RSS 링크는 기본 파란색으로 보인다. 새 CSS를 뒤에 추가하는 것만으로는 해결되지 않는 사례다.
- About 복귀 스크립트는 버튼 DOM보다 먼저 실행되어 return한다. 디자인 문제와 별개지만 탐색 경험을 함께 정리할 때 수정할 결함이다.
- 전체 카테고리·전체 태그 페이지는 주요 메뉴에 직접 진입 링크가 없다. 사이드바에서 개별 분류로는 이동할 수 있으나 전체 분류 탐색의 위치가 모호하다.
- 공사 안내의 유지/제거는 운영 결정이다. 통일성 작업이라는 이유로 임의 삭제하지 않는다.

근거: [about/single.html](layouts/about/single.html), [_custom.scss](assets/css/_custom.scss)의 `.about-link-btn`, [_layouts.scss](assets/css/_layouts.scss)의 전역 `a`, [menu/links.html](layouts/_partials/menu/links.html).

### H. 모바일·404 등 보조 화면

- 모바일은 현재 위치 바·왼쪽 메뉴·상단 목차·전체 화면 검색이라는 최근 구조를 보존한다.
- 목차가 없는 화면의 모바일 바는 현재 카테고리/태그/검색 문맥 대신 사이트 이름을 표시한다. 목록 헤더와 함께 위치 안내 원칙을 정할 대상이다.
- 검색 필터, 긴 카테고리 제목, 많은 태그, 10개 번호의 pagination은 좁은 폭에서 별도 상태 검증이 필요하다.
- [404.html](layouts/404.html)은 공통 base 레이아웃 밖에서 거대한 `40vmin` 숫자와 `Page Not Found`를 출력한다. 같은 사이트라는 인상과 복귀 안내가 약하다.
- 일반 `list.html`·`single.html` fallback도 새로 생기는 페이지에 이전 중앙 정렬 헤더를 노출할 수 있다. 사용하는 URL을 확인하고 기본 페이지 헤더를 정리한다.

## 4. 디자인 불일치를 계속 만드는 코드 구조

| 구조 | 확인한 문제 | 정리 방향 |
| --- | --- | --- |
| 기본 색 + 지역 색 | 기본 `--color-link: #0055bb`, menu/search 지역 청록, custom의 직접 OKLCH 값이 공존 | text/surface/border/accent/focus 같은 역할 토큰을 공통 선언하고 페이지가 참조 |
| CSS 우선순위 | `.list-header p .list-count`의 파란색이 뒤의 `.list-header-count .list-count` 청록색보다 우선. 운영 computed color는 `rgb(0,85,187)` | 덮어쓰기를 늘리지 말고 같은 요소의 규칙을 한 책임 아래 통합 |
| UI와 Markdown 혼합 | 홈·분류·검색 전체 wrapper에 `.markdown`; UI 링크마다 예외 선택자 누적 | 실제 글 본문에만 Markdown 규칙 적용. UI 헤더/목록/필터는 독립 스타일 |
| 폰트 로드와 사용 불일치 | normalize는 Inter/Noto Sans KR 로드, 새 UI는 Pretendard/DM Mono 지정. 두 폰트의 로드 선언은 없음 | 폰트 선택 후 실제 로드·fallback·굵기를 명시. computed family 문자열만으로 로드 성공을 판단하지 않기 |
| 미정의 변수 | `--gray-700` 참조가 있으나 정의가 없음 | 의미 토큰으로 교체하고 hover/focus의 실제 결과 확인 |
| 파일 책임 혼재 | `_search.scss` 1,255줄에 공용 카드·분류·pagination 포함. `_custom.scss` 1,175줄에 읽기/About 전체 포함 | 컴포넌트와 페이지 단위로 책임 분리. 파일 이동 자체를 디자인 개선으로 간주하지 않기 |
| 이전 CSS 후보 | `.categories-tree`, `.categories-list`, `.category-posts`는 현 템플릿·JS에서 참조를 찾지 못함 | 콘텐츠/빌드 결과까지 확인 후 제거 후보로 처리 |
| 렌더링 중복 | 카테고리 템플릿/검색 JS, Hugo pagination/JS pagination, 모달 필터/페이지 필터 | 데이터 계약과 외관 규칙 공유. 모든 것을 JS나 하나의 거대 partial로 통합할 필요는 없음 |
| 반응형 상수 | SCSS breakpoint, JS 77.4rem, 이미지 sizes의 px, About 640px 등이 분산 | 같은 전환 경계는 통일하고 갤러리 등 목적이 다른 경계는 유지 |

seotax 흔적으로 지목한 taxonomy chip·pagination의 기본 구조는 초기 커밋 `1f56de5`의 `_search.scss`에도 존재한다. 반대로 최근 카드·모달·사이드바까지 전부 원본 잔재라고 부르는 것은 부정확하다. 현재는 여러 세대의 규칙이 섞인 상태다.

## 5. 권장 작업 순서

1. **기준표 확정:** 현재 About/포스팅/사이드바에서 폰트, 색, 제목 위계, 링크 상태, 여백, 반경을 추출한다. 화면별 스크린샷을 기준으로 남긴다.
2. **명백한 결함 수정:** categories 본문 전용 렌더링, 중복 ID/비활성 하위 링크, 빈 태그 처리, About 복귀 초기화, 미정의 변수와 충돌 선택자부터 처리한다.
3. **공용 목록 정비:** 페이지 헤더·글 목록·메타데이터·페이지 번호를 함께 바꿔 홈/카테고리/검색에 적용한다. 목록 행 또는 약한 카드 중 한 방향을 선택한다.
4. **분류 탐색 완성:** categories 디렉터리와 tags 전체 보기를 디자인하고 사이트 내 진입 경로·수량 단위를 정한다.
5. **검색 정비:** 모달/결과 페이지의 입력창·필터·선택/빈 상태를 함께 통일한다.
6. **보조 UI 마무리:** series, 이전/다음, 댓글 경계, footer, 404 및 실제 사용 중인 shortcode를 검토한다.
7. **중복 정리:** 확정된 컴포넌트를 기준으로 `_search.scss`·`_custom.scss`를 분리하고 오래된 규칙을 제거한다. 새 override를 계속 덧붙이는 방식은 피한다.

각 단계를 따로 검토할 수 있는 변경으로 만든다. 전체 스타일 파일을 한 번에 다시 쓰면 사용자가 만족하는 화면까지 바뀌는 위험이 크다.

## 6. 구현 전에 필요한 디자인 결정

지금 분석을 위해 답할 필요는 없으며 해당 구현 단계에서 결정한다.

- 글 목록을 선·여백 중심 행으로 바꿀지, 카드 형태를 유지하며 가볍게 할지.
- categories를 전체 주제 디렉터리로 제공할지, 펼침 탐색을 유지할지.
- tags의 기본 정렬과 사이트 메뉴에서 전체 분류로 진입하는 위치.
- 화면 제목/수량 표기의 언어 원칙과 `Total` 집계 의미.
- 실제 사용할 본문/UI 폰트. Pretendard/DM Mono는 현재 선언만 있으므로 선택 시 로드까지 설계해야 한다.

기능 제거, 공사 공지 제거, About 갤러리 배치 변경, 모바일 패널 동작 변경은 이 분석의 권고에 포함하지 않는다.

## 7. Codex 후속 작업에 사용할 요청 예시

> THEME_CONTEXT.md와 DESIGN_AUDIT.md를 읽고 작업해줘. 최근 About·포스팅 상세·카테고리 사이드바를 디자인 기준으로 유지하고, 이번 변경은 [위 단계 하나]에 한정해줘. 새 CSS override를 추가하기 전에 현재 적용되는 규칙과 공유 컴포넌트를 확인해줘. 검색 필터·URL 상태, 카테고리 2단계 경로, 글 진입/복귀 전환, 이미지 확대, About 미리보기, 모바일 메뉴/목차/검색 상호 배타 동작을 보존해줘. 디자인 선택이 필요한 항목은 구체적인 안을 제시해줘. 운영 디렉터리와 deploy.sh를 건드리지 말고 실제 글 몇 개만 /tmp에 축소 빌드해서 검증해줘.

## 8. 검증 범위와 후속 완료 기준

이번 분석에서는 운영 사이트의 홈/categories/tags/About/검색/실제 포스팅을 Chrome 151의 1440px 화면에서 확인하고 템플릿·SCSS·JS·개발 콘텐츠를 대조했다. 검색 모달과 390px의 홈/categories/tags/검색/포스팅도 캡처해 비교했다. 해당 390px 화면에서 문서 전체의 가로 넘침은 관찰되지 않았다. 외부 댓글과 Analytics는 분석 브라우저에서 차단했다. 따라서 댓글 내부 UI를 검증했다고 보지 않는다. 자동화 브라우저의 기본 폰트 환경은 사용자 기기와 다르므로 한글 fallback 외관을 실제 사이트 결함으로 분류하지 않았다.

개발 테마는 실제 글 3개(`esp32-c6-zigbee-home-assistant`, `2023-knou-c-study-7`, `smart-gulbi-3`)와 About/search를 넣어 `/tmp/varofla-design-audit-13syimk_`에 축소 빌드했다. Hugo 0.158.0 extended, Pages 29, Non-page files 55, Static files 79, Processed images 64로 성공했다. 캡처와 측정 스크립트도 같은 임시 디렉터리에 있다. `/tmp` 자료는 영구 보관물이 아니다.

후속 구현의 완료 기준:

- 같은 역할의 제목·링크·태그·필터·선택 상태가 같은 규칙을 사용한다.
- categories에서 중복 ID가 없고 모든 보이는 링크와 버튼이 마우스·키보드로 작동한다.
- 빈 태그가 전체 태그·검색·글 메타데이터 어디에도 나타나지 않는다.
- 카테고리/태그/검색 결과의 수량 단위가 명확하다.
- 390/768/1440px 및 메뉴 전환 경계 양쪽에서 검증한다. 실제 모바일 기기 확인은 별도다.
- 긴 제목, 이미지 없는 글, 여러 태그, 결과 없음, 검색 필터 선택, 페이지 번호 마지막 구간을 포함한다.
- 검색어·분류·태그 AND/OR·페이지 상태와 뒤로가기/새로고침을 보존한다.
- 글 진입/복귀, 목차, 확대 이미지, About 갤러리의 기존 동작을 유지한다.
- 폰트 다운로드와 실제 렌더 폰트를 확인하고 외부 폰트 실패 시에도 읽을 수 있다.
- 코드/표/series/hint/columns가 있는 대표 글에서 본문 스타일 회귀가 없다.
